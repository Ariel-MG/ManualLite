---
topic: ManualLite v2 — núcleo compartido y creación por agentes
slug: manuallite-v2
source: docs/v2-brief.md
updated: 2026-09-15T11:56
---

- (note) Fuente: docs/v2-brief.md. El archivo se corta en un ## vacío tras Siempre; no trae Fronteras, Fuera de alcance ni las tres preguntas abiertas. Se destila lo escrito y se infieren las dos preguntas restantes del problema/enfoque.
- (direction by user) Dolor: la extensión solo graba a mano; las actas de Odoo se producen fuera (Playwright + plan.json + script) y ManualLite entra al final para el PDF. Hueco 1: lógica de formato duplicada entre extensión y scripts. Hueco 2: biblioteca plana sin proyecto ni procedencia, mezcla Odoo con otras apps.
- (direction by user) Enfoque: núcleo compartido (formato, modelo de pasos, anotación, exporters); dos frentes (extensión humana y superficie de agentes); biblioteca por carpeta con procedencia. Un manual de agente y uno grabado a mano son el mismo objeto.
- (constraint by user) El núcleo es la única definición del formato; ningún frente duplica lógica de pasos ni de exportación.
- (constraint by user) Los .manuallite.json ya entregados siguen abriendo sin migración manual.
- (constraint by user) El PDF sigue saliendo solo por scripts/export-pdf.ts.
- (constraint by user) Grabar, editar y exportar siguen funcionando igual al terminar.
- (decision by user) Superficie para agentes: extender el MCP existente (mcp-server/), que hoy solo lee y corrige, con el lado de crear: capturar, anotar y armar. No se abre un canal nuevo.
- (question) Biblioteca: ¿la verdad vive en el filesystem (carpetas=proyectos, procedencia en el archivo) o en IndexedDB con carpetas, siendo el disco solo export?
- (question) Captura de agentes: ¿el MCP orquesta la captura (p.ej. Playwright) o solo arma el .manuallite.json a partir de capturas/pasos que un agente o script externo ya produjo?
- (event) Brief restaurado: docs/v2-brief.md ahora incluye Nunca y Fuera de alcance; el ## vacío era truncado de escritura, no ausencia de secciones.
- (constraint by user) La extensión no corre Playwright. La automatización vive del lado del agente; la extensión no orquesta navegadores.
- (constraint by user) No rediseñar el editor.
- (constraint by user) No tocar src/lib/exporters/pdfLayout.ts.
- (constraint by user) No cambiar el pipeline de captura: ya se resolvió con el side panel.
- (constraint by user) Fuera de alcance: agrupar o partir PDFs (decisión de cliente abierta); nube/sync/multiusuario; publicación web o help center; traducción de manuales.
- (decision by user) Biblioteca: disco. Las carpetas son directorios de proyecto; la procedencia viaja en el archivo. IndexedDB es borrador de la sesión de grabación, no biblioteca.
- (decision by user) Captura: el MCP arma, no navega. Playwright sigue capturando por fuera y llama al MCP para armar. Frontera dura del brief (extensión no orquesta navegadores).
- (decision) Procedencia a nivel de manual, no por paso. Objeto opcional provenance { producedBy: human|agent, producedAt, origin? }. producedBy distingue cómo se produjo el objeto; producedAt es la fecha de producción; origin es un string libre del lado agente (plan, script, skill). Ausente = desconocida (archivos ya entregados). Correcciones y ediciones no reescriben producedBy. formatVersion se queda en 1 (campos aditivos).
- (note) Procedencia por paso se descarta: el dolor del brief es de biblioteca (proyecto + cómo se produjo el manual). Step.createdAt ya existe en el modelo vivo. Autoría mixta por paso no está pedida; inflaría el formato y exigiría migración semántica.
- (capability) CAP-1 Núcleo único: formato, modelo de pasos, anotación y exporters. Ningún frente duplica esa lógica.
- (capability) CAP-2 Grabar, editar y exportar desde la extensión siguen igual, contra el núcleo, con el pipeline de captura del side panel.
- (capability) CAP-3 Un agente crea un manual por el MCP existente: recibe capturas ya tomadas, anota y arma el .manuallite.json en disco.
- (capability) CAP-4 El MCP sigue leyendo y corrigiendo manuales (list/load/image/write_corrected).
- (capability) CAP-5 Biblioteca en disco por carpeta de proyecto, con procedencia en el archivo.
- (capability) CAP-6 Un manual de agente y uno grabado a mano son el mismo objeto: mismos caminos de abrir, editar y exportar. Los .manuallite.json ya entregados abren sin migración.
- (decision) El núcleo es la superficie Node-safe de src/lib (esquema/parse, pasos, anotación, exporters). I/O de navegador (IndexedDB, download, File) no vive ahí. mcp-server deja de espejar el esquema a mano; export-pdf.ts sigue siendo el camino headless del PDF.
- (assumption) No hay raíz mágica de biblioteca: el directorio de proyecto lo elige quien llama (humano o agente), como hoy el MCP recibe dir.
- (assumption) Playwright, plan.json y el runner de Odoo siguen fuera de este repo; este spec no los absorbe, solo deja de duplicar el armado del JSON.
- (event) Self-validate pass 1 (coherence): Spec Law 1-6 y 8 OK. 6 capabilities con intent+success; constraints que doblan; non-goals explícitos; success signal demostrable; IDs CAP-1..CAP-6; prosa magra.
- (event) Self-validate pass 2 (preservation): claims del brief (dolor, enfoque, Siempre, Nunca, Fuera de alcance, MCP, disco, captura, procedencia) aterrizan en SPEC.md o architecture.md. Wrapper-only: incidente de truncado y saludos de skill, registrados en memlog, no en el kernel.
- (event) SPEC.md y architecture.md derivados del memlog.
- (decision by user) Anotación: extraer geometría del anillo a una función que recibe CanvasRenderingContext2D. Extensión sigue con OffscreenCanvas; MCP usa @napi-rs/canvas 1.0.9 (Bun 1.3.14, bun run). No polyfill de globales. La extensión no depende de @napi-rs/canvas.
- (constraint by user) Paridad de anotación visual, no byte a byte. Antialias del filo difiere Skia vs Chrome (media 0.71/canal en caja del halo; línea central #dc2626 idéntica). Se acepta.
- (event) Correct-course: Brownfield de architecture.md era falso (Bun no tiene OffscreenCanvas/createImageBitmap). AD-7 añadido; CAP-1 success aclara paridad visual; IDs CAP-1..CAP-6 intactos. Sin PRD/epics/stories aún.
- (decision by user) Non-goal partido: agrupar procedimientos de un módulo en un solo PDF sale del fuera de alcance de este spec (queda en spec-secciones-numeradas). Partir un manual en varios PDF sigue fuera. La decisión de cliente ya no está abierta.
- (note) bmad-correct-course no corresponde: no hay PRD; epics.md es solo CAP-1 y no menciona agrupar/partir PDF; architecture.md no cambia. Escritor canónico: memlog de este spec, no edición a mano ni sprint-change-proposal.
- (event) Self-validate pass 1 (coherence): único cambio de kernel es el non-goal de PDF; CAP-1..CAP-6 intactos.
- (event) Self-validate pass 2 (preservation): «partir PDF» permanece; «agrupar procedimientos de un módulo en un solo PDF» sale y apunta a spec-secciones-numeradas. Fuente docs/v2-brief.md alineada.
- (event) SPEC.md rederivado del memlog. architecture.md sin cambios.
